defmodule AshLearning.Repo.Migrations.AddFullNameToUserIdentities do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:user_identities) do
      add :full_name, :text
    end
  end

  def down do
    alter table(:user_identities) do
      remove :full_name
    end
  end
end
